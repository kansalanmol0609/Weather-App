<!DOCTYPE html>

<html>
    <head>
        <title>Error Page</title>
        <link rel="icon" href="./img/icon.png">
        <link rel="stylesheet" href="./css/styles.css">
    </head>
    <body>
        <div class="main-content">
            {{>header}}
            <p>Error 404: Nothing to see here!!</p>
        </div>
        {{>footer}}
    </body>
</html>